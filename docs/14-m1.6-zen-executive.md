# M1.6 ZEN EXECUTIVE Acceptance

## Scope

M1.6 replaces only the web presentation layer. HomeComposition, Agent/Tool registries, deterministic metrics, provenance, RBAC/RLS, SyntheticAdapter, API contracts and the read-only execution boundary remain unchanged.

Direction contract: `m1-6-zen-executive-f46862fe`.

## Implemented Result

- Warm Light is the default visual world, with explicit Dark and System preferences.
- The home opens with one editorial Jarvis judgment, a compact fact line, typographic operating metrics and three ranked actions.
- The inspector starts closed. It opens only from the toolbar, an action or “查看依据”.
- Evidence summaries show human-readable source and update time; raw identifiers remain inside the inspector.
- The left rail exposes only 今日运营, 会话, 设置 and 帮助. Future workbench destinations live in Help rather than the formal navigation.
- The persistent paper composer keeps marketplace, business date and read-only synthetic boundaries visible.
- Styles are split into tokens, shell, navigation, composer, inspector, components and responsive modules. Global CSS contains only reset, body and accessibility rules.

## Visual Tokens

The shipped palette uses Warm Ivory `#F4F1EA`, Navigation Paper `#ECE8DF`, Surface `#FAF8F3`, Elevated Paper `#FFFDFC`, Ink `#292722`, Moss `#6F7868`, Champagne Bronze `#A58B63` and Risk Clay `#A86355`.

The specified muted color remains a palette token. Visible small text uses `--text-subtle: #645F56`, measured at 5.62:1 against the canvas, and no visible metadata is intentionally set below 11px.

## Responsive Evidence

The live app was checked at the required viewports:

| Viewport | Result |
| --- | --- |
| 1440 x 900 | Quiet single-canvas executive judgment; inspector collapsed by default; two ranked actions visible above composer |
| 1024 x 820 | Navigation remains available; first ranked action visible above composer |
| 768 x 820 | Navigation becomes an accessible drawer; no horizontal overflow |
| 375 x 812 | All three actions precede supporting metrics; third action ends at 660px and composer begins at 717px; document width remains 375px |

The QA raster files are stored under `.impeccable/review/` for local inspection and are not a substitute for the running product.

## Safety And Data Truth

- All demo values come from the seeded `2026-08-31` synthetic business day and remain `synthetic=true`.
- `NEXT_PUBLIC_DEMO_BUSINESS_DATE` can select another available day; no missing day is synthesized in the browser.
- The UI contains no Amazon write control. Suggestions and approval drafts remain review-only.
- Home content continues to render only registered HomeComposition components.

## Verification

- Impeccable direction detector: clean (`[]`).
- Frontend: 19 tests passed.
- API: 38 passed, 2 skipped.
- Infrastructure/security contracts: 19 passed.
- Next.js production build: passed.
- Browser console at the final reviewed state: no errors or warnings.
- Docker CLI was unavailable in the desktop shell; Compose structure and health contracts were covered by infrastructure tests.
